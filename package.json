{
  "name": "perf-optimizer",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild agent.js --bundle --platform=node --format=esm --outfile=bundle.mjs --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\""
  },
  "dependencies": {
    "deepagents": "^1.8.0",
    "@langchain/anthropic": "^1.3.18",
    "@langchain/core": "^1.1.25"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
